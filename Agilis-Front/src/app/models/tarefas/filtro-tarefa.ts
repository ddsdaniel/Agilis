export interface FiltroTarefa {
  releaseId: string;
  sprintId: string;
  relatorId: string;
  solucionadorId: string;
  clienteId: string;
  produtoId: string;
  featureId: string;
  tag: string;
}
